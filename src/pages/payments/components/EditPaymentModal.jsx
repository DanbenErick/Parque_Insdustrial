const Field = ({ label, icon, children }) => <label className="flex flex-col gap-1"><span className="text-[10px] font-bold text-on-surface-variant uppercase tracking-wider pl-0.5">{label}</span><span className="relative">{icon && <span className="material-symbols-outlined absolute left-2.5 top-1/2 -translate-y-1/2 text-on-surface-variant text-[16px]" translate="no">{icon}</span>}{children}</span></label>;

const EditPaymentModal = ({ payment, method, onMethodChange, operation, onOperationChange, date, onDateChange, onSubmit, onClose, isSubmitting, formatCurrency, backdropProps, contentProps }) => {
  if (!payment) return null;
  return <div {...backdropProps} className="fixed inset-0 z-[70] flex items-center justify-center p-4 bg-slate-900/60 backdrop-blur-sm"><div {...contentProps} className="bg-surface rounded-2xl shadow-2xl w-full max-w-md flex flex-col overflow-hidden border border-outline-variant animate-in fade-in zoom-in-95 duration-150"><div className="px-5 py-4 border-b border-outline-variant bg-amber-500/10 flex justify-between items-center"><h3 className="text-base text-amber-900 font-bold flex items-center gap-2"><span className="material-symbols-outlined text-[22px] text-amber-700" translate="no">edit_square</span>Modificar Información del Pago</h3><button type="button" onClick={onClose} disabled={isSubmitting} className="w-7 h-7 rounded-full hover:bg-surface-variant flex items-center justify-center text-on-surface-variant disabled:opacity-50" aria-label="Cerrar"><span className="material-symbols-outlined text-[18px]" translate="no">close</span></button></div><form onSubmit={onSubmit} className="p-5 space-y-4"><div className="bg-surface-container-low border border-outline-variant/60 rounded-xl p-3.5 space-y-2 text-xs">{[['Socio:',payment.socio],['Recibo / Comprobante:',payment.numero_comprobante],['Monto Pagado:',`S/ ${formatCurrency(payment.monto_pagado)}`]].map(([label,value]) => <div key={label} className="flex justify-between"><span className="text-on-surface-variant font-medium">{label}</span><span className="font-bold text-on-surface">{value}</span></div>)}</div><div className="space-y-3"><Field label="Método de Pago *" icon="account_balance"><select className="appearance-none w-full border border-outline-variant rounded-md pl-8 pr-8 py-1.5 h-8 bg-white text-xs font-medium" value={method} onChange={(event) => onMethodChange(event.target.value)} required><option value="Transferencia">Transferencia</option><option value="Efectivo">Efectivo</option><option value="Cheque">Cheque</option><option value="Depósito">Depósito en Cuenta</option></select></Field><Field label="Nº Operación / Referencia" icon="tag"><input type="text" className="w-full border border-outline-variant rounded-md pl-8 pr-3 py-1.5 h-8 bg-white font-data-mono text-xs" value={operation} onChange={(event) => onOperationChange(event.target.value)} placeholder="Ej. 04829102" /></Field><Field label="Fecha de Pago *"><input type="date" className="w-full border border-outline-variant rounded-md px-3 py-1.5 h-8 bg-white font-data-mono text-xs" value={date} onChange={(event) => onDateChange(event.target.value)} required /></Field></div><div className="flex justify-end gap-2 pt-3 border-t border-outline-variant"><button type="button" disabled={isSubmitting} onClick={onClose} className="px-4 py-2 text-xs font-semibold text-on-surface-variant hover:bg-surface-variant rounded-md">Cancelar</button><button type="submit" disabled={isSubmitting || !date || !method} className="px-4 py-2 text-xs font-bold text-white bg-amber-600 rounded-md shadow-sm flex items-center gap-1.5 disabled:opacity-50"><span className={`material-symbols-outlined text-[16px] ${isSubmitting ? 'animate-spin' : ''}`} translate="no">{isSubmitting ? 'sync' : 'save'}</span>{isSubmitting ? 'Guardando...' : 'Guardar Cambios'}</button></div></form></div></div>;
};

export default EditPaymentModal;
